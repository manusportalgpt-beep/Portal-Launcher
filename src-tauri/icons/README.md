# Portal Launcher square icon source

The master source is `portal-square-source.svg`. It keeps the red multi-arc portal motif but arranges the three broken arcs as a readable square mark on a near-black tile. All platform icon formats in this folder are generated from this source.
